import React from "react";
import ReactDOM from "react-dom/client";
import TradingAnalyticsPrototype from "./pages/index";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TradingAnalyticsPrototype />
  </React.StrictMode>
);